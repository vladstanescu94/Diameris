/**
 * Onboarding — the 7-screen flow with its 5-dot indicator.
 *
 * The shell renders `<OnboardingFlow>` when `route.kind === 'onboarding'`; everything else
 * in here is internal to the feature.
 */

export { OnboardingFlow } from './OnboardingFlow'
export type { OnboardingFlowProps } from './OnboardingFlow'
export type { OnboardingDraft, SavingsSliderPosition } from './types'
